# 변경 이력

## 1.6.0 — 2026-09-21

### 고침 — 차트 팔레트를 테마별 두 벌로 가른다

"차트 색이 너무 칙칙하다"의 원인을 재고 고쳤다.

**원인은 한 팔레트로 라이트·다크를 동시에 만족시킨 것이다.** 두 캔버스 모두에서 3:1을
넘는 명도 구간은 전체의 18%(v=100~140)뿐이라, 여섯 색을 그 띠에 욱여넣으면 명도가
서로 같아지고 같은 명도로 다른 색상을 만들려면 채도를 깎게 된다. 텍스트 대비 규칙을
범주형 팔레트에 잘못 적용한 것이다.

| | v1.5.2 | v1.6.0 |
|---|---|---|
| 평균 채도 C* | 38 | **56 / 58** |
| 명도 L* 범위 | 43~54 | **42~57 / 60~78** |
| 최소 ΔE(서로 갈림) | 15.6 | **19.9 / 23.6** |
| 캔버스·카드 3:1 | 6/6 | 6/6 |

색상각은 Figma 기본 팔레트에서 가져왔고(참조 톤), 명도만 캔버스에 맞춰 옮겼다.
색상각이 고정이라 테마를 바꿔도 "3번은 초록"이 유지된다.

`--ez-chart-muted`는 다크에서 gray-60 → **gray-50**이다. 60은 2.91:1로 미달이었다.

#### 깨질 수 있는 것

`--ez-chart-*`를 참조만 하고 있으면 **아무것도 안 해도 된다** — 값이 바뀌었을 뿐이다.
hex를 복사해 화면에 박아 뒀다면 토큰 참조로 바꾼다. 다크에서 차트가 라이트 값으로
나온다면 그쪽이다.

#### 생성 규칙

`chartPalette(scheme)`가 LCh에서 뽑는다. HSL이 아닌 이유는 같은 L*이면 색상이 달라도
눈에 같은 밝기로 보이기 때문이다 — HSL의 L에는 그 성질이 없다. 흰 캔버스는 L* 58이
천장이라 라이트 쪽 명도 폭은 아래로 만든다. 채도 상한 C* 66 — 색역 끝까지 밀면 형광이 된다.

`generate-tokens.mjs`는 다크 셀렉터를 경계로 잘라 각 블록에 자기 팔레트를 쓴다.
전역 치환이면 다크가 라이트 값으로 덮인다.

#### 색각이상

색만으로는 못 푼다. 2색각은 명도·청황 두 축만 보므로 여섯 범주를 색상으로 가르는 것은
불가능하다 — 후보 여럿을 Brettel/Viénot 투영으로 시뮬레이션해도 적록 ΔE가 20을 넘는
6색 조합은 나오지 않는다. 그 몫은 `aria.decal` 패턴이 진다.

## 1.5.2 — 2026-09-20

### 원칙 — §10 「공개 클래스 이름은 API다」를 추가한다

v1.5.1은 [도입](/adoption) 문서에 주의만 적었다. 규칙을 원칙 문서 본문으로 올린다.

- **§10 신설** — 내는 쪽·쓰는 쪽·검사 셋으로 나눴다. 이름을 지을 때의 기준
  (프리미티브는 보편명, 구체적인 것에는 한정어)도 함께 적었다
- **§9에 ⑧ 추가** — 새 컴포넌트 절차에서 이름 충돌을 보게 한다
- 종전 §10(리뷰 질문 다섯)은 **§11**로 밀렸다

### 고침 — 죽은 문서 앵커 2건

VitePress 슬러그가 한글 제목을 **NFD**(자모 분해)로 내보낸다. 소스는 NFC라 눈으로는
같은데 브라우저가 fragment를 정규화하지 않아서, 손으로 쓴 `#한글-제목` 링크가 조용히
죽는다. `foundations.md`에 한 건이 이미 그렇게 살아 있었다.

슬러그 생성을 바꾸는 쪽은 실패했다 — 기본 slugify가 번호·구두점까지 처리하고 있어
갈아끼우면 앵커 114개가 한꺼번에 깨진다. **생성은 그대로 두고 결과를 검사한다**:
`npm run check:anchors`를 만들어 `verify`에 걸었다.

## 1.5.1 — 2026-09-20

### 문서 — `.ez-*` 레이아웃 클래스가 예약어임을 명시한다

코드 변경 없음. [도입](/adoption) §1-6을 추가했다.

v1.4.0에서 레이아웃 프리미티브 일곱을 넣으면서 `.ez-grid`가 공개 이름이 됐는데,
소비 프로젝트가 같은 이름을 다른 뜻으로 쓰고 있으면 **조용히 깨진다**는 것을
알리지 않았다. 어드민 목업에서 실제로 났다 — Tabulator 래퍼가 `.ez-grid`였고,
의존성을 올린 순간 표 본문 높이가 0px가 됐다. 콘솔 오류도 타입 오류도 없었다.

예약어 목록을 문서에 박지 않고 `dist/layout.css`에서 읽는 방법과, CI에 걸 검사
위치를 함께 적었다.

## 1.5.0 — 2026-09-20

### 고침 — 밀도 토큰이 역할 토큰을 참조한다

v1.3.0 밀도 토큰과 v1.4.0 역할 토큰이 **같은 역할에 다른 값**을 들고 있었다.
화면은 밀도 토큰을 쓰고 원칙 문서는 역할 토큰을 말해서 둘이 갈라졌다(감사 H2).

| | v1.4.0까지 | v1.5.0 |
|---|---|---|
| `--ez-card-padding` | `space-4`(16) | `gap-block` → **20 / 24** |
| `--ez-card-gap` | `space-4`(16) | `gap-region` → **24 / 32** |
| `--ez-section-gap` | `space-4`(16) | `gap-region` → **24 / 32** |

역할이 상위 개념이고 밀도는 그 값을 바꿀 뿐이다. 이제 밀도 블록이 바꾸는 것은
역할 간격 셋(`inter`/`block`/`region`)과 모양 셋(radius/border/shadow)뿐이다.

**여백이 전반적으로 한 단계 넓어진다.** 감사에서 "전 화면이 사다리보다 한 단계 낮다"로
잡힌 것의 구조적 원인이 이것이었다.

### 추가 — `--ez-space-0-5` (2px)

스와치 띠처럼 '거의 붙은' 간격. 여기 아래는 0이다.

## 1.4.0 — 2026-09-20

### 추가 — 설계 원칙 문서 (`docs/principles.md`)

토큰 값은 다 있는데 화면이 만족스럽지 않은 원인은 **값을 어디에 얼마나 쓸지 정하는 규칙이
없었던 것**이다. `--ez-space-4`가 16px인 건 알아도 이 자리가 4인지 6인지는 매번 감이었다.

Every Layout · Refactoring UI · Atlassian · Carbon · Laws of UX · NN/g를 조사해
업무 화면 맥락으로 좁혔다. 핵심은 §8·§9의 **절차**다 — 따라 하면 일관된 결과가 나오게.

### 추가 — 여백 역할 토큰

스케일(`--ez-space-*`)이 "무엇이 있는가"라면 이쪽은 **"어느 것을 쓸지에 대한 답"**이다.

| 토큰 | 역할 | compact | comfortable |
|---|---|---|---|
| `--ez-gap-adjacent` | 아이콘–라벨, 뱃지 내부 | 4 | 4 |
| `--ez-gap-intra` | 라벨–입력, 제목–본문 | 8 | 8 |
| `--ez-gap-inter` | 필드끼리, 버튼끼리 | 16 | 20 |
| `--ez-gap-block` | 카드 안 섹션끼리 | 20 | 24 |
| `--ez-gap-region` | 카드끼리, 조회–목록 | 24 | 32 |

지켜야 하는 부등식 하나 — **그룹 사이 ≥ 그룹 안 × 2**. 같은 값이면 그룹이 보이지 않는다.

### 추가 — 레이아웃 프리미티브 (`@ezwel/ui/layout.css`)

화면은 일곱 개로만 조립한다: `ez-stack` `ez-cluster` `ez-split` `ez-grid`
`ez-sidebar` `ez-box` `ez-center`.

새 화면에서 `position: absolute`나 일회용 margin이 나오면 예외가 필요한 게 아니라
**부품이 모자란 것**이다. 전부 `--gap`으로 역할 토큰을 받는다 — 숫자를 직접 넣지 않는다.

### 추가 — `--ez-measure` (68ch)

읽는 텍스트의 최대 폭. 글자 수 단위라 글자 크기가 바뀌면 같이 따라온다.

## 1.3.0 — 2026-09-19

핀테크 대시보드 UI 킷(Finebank)을 톤 기준으로 분석하고, 그중 **값이 아니라 구조로
가져올 수 있는 것**만 토큰으로 옮겼다. 색 계열은 이미 같았고 벌어지는 건 밀도와 위계였다.

### 추가 — 밀도 스위치 `data-density="comfortable"`

화면 성격이 다르면 여백도 달라야 한다. 기본은 compact(목록·폼), 대시보드·통계는 comfortable.

| 토큰 | compact | comfortable |
|---|---|---|
| `--ez-card-padding` | 16 | **24** |
| `--ez-card-gap` | 16 | **24** |
| `--ez-card-radius` | 12 | **16** |
| `--ez-card-border-width` | 1 | **0** |
| `--ez-card-shadow` | 없음 | raised |
| `--ez-section-gap` | 16 | 24 |

여유 밀도에서 **카드 테두리를 버린다.** 여백이 충분하면 선이 필요 없고, 선과 여백을
같이 쓰면 시끄럽다.

### 추가 — 숫자 단계 `--ez-font-size-3xl` / `-4xl` (28 / 34px)

KPI 값·금액 전용. 라벨을 낮추고 값을 올리면 위계가 크기 대비만으로 선다.

### 추가 — `--ez-chart-muted`, 2색 기본 원칙

비교 쌍(이번 기간 vs 지난 기간)이 업무 차트의 대부분이다. 계열이 둘이면
`--ez-chart-1` + `--ez-chart-muted` 두 색만 쓰고, 여섯 색은 셋 이상일 때 꺼낸다.

참조 킷은 더 밝은 회색을 쓰지만 그 값은 면 대비 3:1을 못 넘는다 — **톤만 가져오고
값은 우리 스케일에서 뽑았다**(step 40 = 3:1). 검사 326쌍 → 334쌍, 미달 0건.

### 추가 — 부분 다크 `data-scheme="dark"`

셸(헤더·LNB)만 어둡고 본문은 밝은 배치를 위해서다. 루트 전체를 뒤집으면 그게 안 된다.
다크 의미 토큰 블록이 `:root[data-theme='dark']`와 `[data-scheme='dark']`를 함께 받는다.

### 추가 — `--ez-radius-xl` (16px)

## 1.2.0 — 2026-09-19

### 추가 — 범주형 차트 팔레트 `--ez-chart-1` … `-6`

대시보드 도넛이 의미색(success 초록 · danger 빨강 · warning 주황)을 범주 색으로 쓰고 있었다.
채널 구성에 빨강·초록이 섞이면 **"나쁨/좋음"으로 읽힌다.** 범주는 다른 팔레트를 써야 한다.

- 채도를 낮췄다 — 완전 채도 6색이 나란히 있으면 어느 계열도 눈에 안 들어온다
- 휘도를 두 밴드로 번갈아 뒀다 — 인접 색상이 비슷해도 밝기로 갈린다
- 여섯 색 모두 **라이트 캔버스·다크 면 양쪽에서 3:1 이상**(WCAG 1.4.11)이라 테마별 두 벌이 없다

`scripts/check-contrast.mjs`가 차트 색까지 검사한다 — 278쌍 → **326쌍**, 미달 0건.

## 1.1.0 — 2026-09-19

### 고침 — 대비 약속의 기준면을 캔버스로 내렸다 (시각 변경 있음)

v1.0.0은 step 번호를 **흰 배경** 대비비로 정의했다. 그런데 글자가 실제로 놓이는 가장 어두운
면은 캔버스(`--ez-surface-canvas` = gray-10)다. 그래서 카드 안에서는 통과하고 페이지 배경
위에서만 미달하는 구멍이 났다.

어드민 목업 스파이크에서 화면을 렌더해 재다가 드러났고(`ez-admin-design-system`),
검사 스크립트를 붙이자 **의미 토큰 조합 278쌍 중 42건**이 미달이었다.

| 증상 | v1.0.0 | v1.1.0 |
|---|---|---|
| `--ez-text-muted` 캔버스 위 | 3.90:1 | 4.6:1 |
| `--ez-color-gray-50` (약속 4.5) | 4.48:1 | 4.6:1 |
| `--ez-color-danger-50` 위 흰 글자 | 4.4999:1 | 5.3:1 |
| 다크 `--ez-text-muted` on raised | 4.44:1 | 5.8:1 |
| 검사 미달 총계 | 42건 | **0건** |

기준면을 가장 어두운 면으로 내리면 더 밝은 면에서는 자동으로 통과한다. 그리고 한 기준이
두 용도를 함께 만족시킨다 — 밝은 면 위의 어두운 글자, 색 면 위의 흰 글자.

**결과적으로 step 40 이상이 전부 조금씩 어두워진다.** 보조 텍스트·CTA·상태 뱃지의 색이 바뀐다.

### 바뀜 — 사내 표준 주색을 스케일에서 뺐다

`#009782`를 `--ez-color-primary-40`에 앵커했었지만, 캔버스 기준 2.6:1이라 step 40의
약속(3:1)을 만족하지 못한다. **`--ez-brand`로 분리**했고 로고·큰 면적 장식에만 쓴다.
스케일은 색상(hue 171.7°)만 계승한다 — 나란히 놓으면 같은 초록으로 읽힌다.

### 바뀜 — 다크 테마 면을 램프 위로 올렸다

손으로 고른 hex(`#1a1d20`, `#2c3035`)를 쓰던 자리가 검사 대상 밖으로 새고 있었다.
전부 `--ez-color-gray-*` 참조로 바꿨다.

- `surface-raised` / `surface-hover` → gray-80
- `surface-sunken` / `surface-disabled` / `field-bg` → gray-95
- 다크 `text-muted` 40 → 30, `icon-*` 한 단계씩, `field-placeholder` 50 → 40

### 추가

- `scripts/palette.mjs` — 램프 생성(HSL 이분탐색, 양자화 대비 2% 여유)
- `scripts/generate-tokens.mjs` — `npm run palette`로 tokens.css primitive 값을 다시 씀
- `scripts/check-contrast.mjs` — `npm run check:contrast`로 278쌍 검사. `verify`에 물렸다
- `--ez-brand`

### 올릴 때

값이 바뀌므로 화면을 한 번 훑어야 한다. 구조·이름은 그대로라 코드 수정은 없다.
`--ez-color-primary-40`을 브랜드 원색으로 쓰고 있었다면 `--ez-brand`로 바꾼다.

## 1.0.0 — 2026-09-19

최초 배포. 토큰 2층 구조, Vue 3 컴포넌트 14종, 컴포저블 6종, VitePress 문서 사이트.
